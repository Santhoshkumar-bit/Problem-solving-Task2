input_string = ("guvi geek network solutions")

output_string = ""

comparing_string = "Solutions Network Geek Guvi"

# Check if lengths are different, they can't be anagrams
if len(input_string) != len(comparison_string):
